# Installation

The installation includes this web module and the corresponding daemon.

## From source

1. Clone the repository into `/usr/share/icingaweb2/modules/servicenow`

2. Enable both modules using the `Configuration → Modules` menu or the `icingacli`

3. Configure the daemon and database connection resource
